//Operador ternario sintaxis
(condition) ? true : false; //Solo puede almacenar dos resultados

//Condicional IF-ELSE sintaxis
if (condition) {
    //Código si se cumple la primera condición
} else if (condition2) {
    //Código si se cumple la segunda condición
} else {
    //Codigo si no se cumple ninguna de las anteriores
}